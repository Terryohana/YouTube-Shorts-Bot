# .gitignore for YouTube Uploader credentials
# These files must NEVER be committed to version control.

# OAuth client secret downloaded from Google Cloud Console
client_secrets.json

# Cached OAuth token (auto-generated after running authenticate.py)
token.pickle
token.json

# Any backup credential files
*.pickle
*.json
